# 쌤툴(SsamTool) 개선 작업 지시서

> Claude Code / Antigravity 전달용
> 리포지토리: `kfltchJUNO/ssamtool` · 스택: Next.js 14 App Router + Firebase(Firestore/Auth) + Tailwind + Vercel

---

## 0. 작업 원칙 (반드시 먼저 읽을 것)

1. **추측하지 말고 먼저 읽어라.** 각 태스크를 시작하기 전에 관련 파일을 전부 읽고, 현재 구현이 어떻게 되어 있는지 요약해서 보고한 뒤 수정에 들어간다.
2. **원인을 특정한 뒤 고쳐라.** 특히 Task 1은 "이럴 것 같다"로 패치하지 말고, 재현 코드나 로그로 원인을 확정한 다음 수정한다.
3. **파일은 전체 내용으로 출력한다.** diff나 부분 수정이 아니라, 경로를 명시하고 파일 전체를 보여준다.
4. **Firestore 규칙은 코드로 배포하지 않는다.** 규칙 변경이 필요하면 콘솔에 붙여넣을 최종 규칙 텍스트만 별도로 출력한다. (로컬 rules 파일 인코딩 이슈로 CLI 배포 금지)
5. **Firestore에 `undefined`를 절대 넣지 않는다.** 값이 없으면 필드 자체를 생략한다.
6. 분필 차감 로직은 항상 `isChalkEnabled()`(= `settings/monetization` 문서 확인) 뒤에서만 동작해야 한다.
7. 한 번에 한 태스크씩 진행한다. Task 1~3 → Task 4~5 → Task 6 → Task 7 순서로 세션을 나눌 것을 권장한다.

---

## Task 1. 자리표 랜덤 배치 누락 버그 (최우선)

### 증상
학생 16명이 등록된 반에서 랜덤 자리 배치를 실행하면 **일부 학생이 어떤 책상에도 배정되지 않는다.**

### 지시

먼저 자리표 관련 컴포넌트/유틸(`SeatingChart`, `Seat*`, `layout` 관련 파일 전부)을 읽고 배치 알고리즘을 찾아 **현재 로직을 그대로 설명**하라. 그다음 아래 후보를 하나씩 검증해서 실제 원인을 특정하라.

**원인 후보 체크리스트**

| # | 의심 지점 | 확인 방법 |
|---|---|---|
| 1 | **동명이인** — 학생을 이름 문자열로 식별해서 배정/제외하고 있는가? `assigned.includes(name)`, `filter(s => s !== name)` 같은 패턴이 있으면 동명이인 1명이 통째로 사라진다. | 학생 배열이 `string[]`인지 `{id, name}[]`인지 확인 |
| 2 | **책상 수 < 학생 수** — 그리드에 켜진 책상이 15개인데 학생이 16명이면 조용히 잘린다. | 배치 직전 `desks.length`와 `students.length` 로그 |
| 3 | **순회 중 배열 변형** — `forEach`/`for` 안에서 `splice()`나 `shift()`로 원본을 줄이면 인덱스가 밀려 건너뛴다. | 셔플 이후 배정 루프 확인 |
| 4 | **교탁 셀 오계산** — 교탁 위치가 책상 배열에 포함되어 한 칸을 잡아먹는가? | `teacherPos`와 `desks` 좌표 중복 확인 |
| 5 | **중복 desk.id** — 책상 추가/삭제를 반복하면 id가 겹칠 수 있고, Map/객체로 모으면 덮어쓰기가 발생한다. | `new Set(desks.map(d => d.id)).size === desks.length` 검사 |
| 6 | **얕은 복사 누락** — 셔플 배열을 `pop()` 하면서 다른 곳에서도 같은 참조를 소비하는가? | 셔플 결과 배열의 참조 추적 |

**수정 요구사항**

- 학생 식별은 **반드시 고유 ID 기준**으로 바꾼다. 이름 문자열로 비교/제외하는 코드가 있으면 전부 제거한다. 학생 데이터에 안정적인 id가 없다면 반 저장 시점에 생성해 부여하고, 기존 저장 데이터에 대한 마이그레이션(읽을 때 id 없으면 생성해서 채워넣기)도 함께 넣는다.
- 셔플은 **Fisher–Yates**로 구현한다. `sort(() => Math.random() - 0.5)`는 편향이 있으므로 쓰지 않는다.
- 배치 함수는 순수 함수로 분리하고(`src/lib/seating.ts` 등), 입력·출력을 명확히 한다:
  ```ts
  assignSeats(students: Student[], desks: Desk[], options): AssignResult
  // AssignResult: { assignments: Map<deskId, studentId>, unassigned: Student[], emptyDesks: Desk[] }
  ```
- **미배정 학생이 생기면 절대 조용히 넘어가지 않는다.** UI에 명시적으로 경고한다:
  - 학생 > 책상: `"책상이 2개 부족해요. 김OO, 이OO 학생이 배정되지 않았습니다."` + 미배정 학생 이름을 화면에 목록으로 표시 + "책상 추가하기" 버튼
  - 책상 > 학생: 남는 책상은 빈 자리로 표시 (경고 아님)
- 배치 직후 **자체 검증**을 넣는다. 배정된 학생 수 + 미배정 학생 수 = 전체 학생 수가 아니면 개발 콘솔에 에러를 남긴다.

**테스트 케이스** (`src/lib/seating.test.ts` 또는 최소한 임시 검증 스크립트로 확인하고 결과를 보고할 것)

- 학생 16 / 책상 16 → 미배정 0, 중복 배정 0
- 학생 16 / 책상 20 → 미배정 0, 빈 책상 4
- 학생 16 / 책상 12 → 미배정 4명이 정확히 반환됨
- **동명이인 포함 16명** (예: "김민수" 2명) → 둘 다 서로 다른 책상에 배정됨
- 1000회 반복 실행 → 매번 미배정 0 (책상 충분한 경우)

---

## Task 2. 레이아웃과 배치의 분리 저장 (재배치 시 교실 구조 고정)

### 문제
지금은 자리표를 만들 때마다 책상 배치를 처음부터 다시 그려야 한다. 교실 구조는 학기 내내 그대로인데 매번 반복 작업이 발생한다.

### 지시

**교실 레이아웃(구조)** 과 **자리 배정(누가 어디)** 을 완전히 분리한다.

```
layouts/{layoutId}          ← 교실 구조 (거의 안 바뀜)
  name: "302호 ㄷ자"
  cols, rows
  desks: [{ id, x, y }]     ← studentName 필드 제거
  teacherPos: { x, y }
  createdAt, updatedAt

seatingCharts/{chartId}     ← 특정 시점의 배정 결과
  layoutId                  ← 위 레이아웃 참조
  classId
  title: "10월 2주차"
  assignments: [{ deskId, studentId }]
  createdAt
```

> 기존 `layouts` 문서에 `studentName`이 박혀 있다면, 읽을 때 감지해서 새 구조로 변환하는 마이그레이션을 포함할 것. 기존 사용자의 데이터가 깨지면 안 된다.

**UI 요구사항**

- 자리표 탭 진입 시 저장된 레이아웃 목록을 먼저 보여준다. 새로 만들기는 부차 동작.
- 레이아웃 선택 후에는 **"구조 잠금" 상태가 기본값**이다. 잠금 중에는 책상 추가/삭제가 비활성화되고, 상단에 `🔒 구조 고정 · [편집하기]` 표시.
- **"학생만 다시 섞기"** 버튼을 최상위 액션으로 배치한다. 이게 가장 자주 쓰이는 동작이다.
- 개별 학생 자리 교환은 드래그 앤 드롭으로 유지.
- 레이아웃 복제 기능 (`302호 ㄷ자` → `302호 ㄷ자 (모둠형)`).

**추가 배치 옵션** (구조 잠금과 함께 제공)

1. **지난 배치와 다르게** — 같은 `classId`의 최근 `seatingChart`를 불러와, 직전과 동일한 자리를 최대한 피해서 배정한다. (완전 회피가 불가능하면 겹침 최소화 후 "3명은 같은 자리예요"로 안내)
2. **태그 분산** — 학생에게 태그(국적/모국어 등)를 달 수 있게 하고, 같은 태그끼리 인접(상하좌우)하지 않도록 배정한다. 완벽한 해가 없으면 제약 위반 수를 최소화하고 위반 지점을 UI에 표시한다.
3. **분리 지정** — 특정 두 학생을 인접시키지 않는 규칙을 반별로 저장.

> 2, 3번은 백트래킹 또는 제약 위반 수를 스코어로 두고 N회 시도 후 최선을 고르는 방식으로 구현하면 충분하다. 완벽한 최적화는 필요 없다.

---

## Task 3. 용지 크기에 맞춘 인쇄

### 문제
자리표 인쇄 시 종이 크기를 넘어가거나 여백이 이상하게 남는다.

### 지시

- 인쇄 옵션 UI 추가: **용지(A4 / A3 / Letter) × 방향(세로 / 가로)**
- `@page { size: A4 landscape; margin: 10mm; }` 를 선택값에 따라 동적으로 주입한다.
- **자동 맞춤 스케일링**: 레이아웃의 실제 픽셀 크기와 인쇄 가능 영역(용지 크기 − 여백)을 비교해 `scale = min(printW / contentW, printH / contentH, 1)` 을 계산하고 `transform: scale()` + `transform-origin: top left`로 적용한다. 확대는 하지 않고 축소만 한다.
- 인쇄 미리보기에 실제 용지 비율의 흰 배경을 그려서, 내용이 종이에 어떻게 앉는지 보이게 한다. **"A4 세로 1장에 들어갑니다"** 같은 안내 문구 표시.
- 교실이 커서 1장에 안 들어가면 축소 비율을 알려주고(`85%로 축소됨`), 너무 작아지면(60% 미만) 가로 방향 또는 A3를 제안한다.
- **선생님 시점 / 학생 시점** 토글 유지 (좌우 반전).
- 인쇄 시 제외할 요소는 `.no-print` 클래스로 통일한다.

---

## Task 4. 학생 메모지 + 자리표 통합 인쇄

### 지시

학생 메모 인쇄 화면에 인쇄 형식 선택을 추가한다.

```
인쇄 형식
  ○ 목록형        — 학생 명단 순서대로 메모 칸
  ○ 자리표형      — 교실 배치 그대로, 각 책상 칸 안에 메모 공간
  ● 자리표 + 목록 — 1페이지: 자리표 / 2페이지 이후: 목록형 메모지
```

- 자리표형은 저장된 `seatingChart`를 선택해서 불러온다. (반 선택 → 저장된 자리표 목록 → 선택)
- 자리표가 아직 없으면 "먼저 자리표를 만들어주세요" 안내와 함께 자리표 탭으로 이동하는 링크를 제공한다.
- 각 책상 칸에는 학생 이름 + 체크박스 몇 개(발음/문법/태도) + 자유 메모 줄을 넣는다. 항목은 설정에서 켜고 끌 수 있게 한다.
- 페이지 나눔은 `break-after: page`로 제어하고, 메모 칸이 페이지 경계에서 잘리지 않게 `break-inside: avoid`를 적용한다.
- Task 3의 용지 맞춤 로직을 그대로 재사용한다. **인쇄 관련 코드를 공용 유틸(`src/lib/print.ts`)로 추출**해서 이름표·자리표·메모지가 같은 함수를 쓰게 리팩터링할 것.

---

## Task 5. 분필 부족 시 에러 처리 (버그)

### 증상
분필이 없는 상태에서 기능을 실행하면 사용자에게 이렇게 표시된다:

```
Unexpected token '<', "<!DOCTYPE "... is not valid JSON
```

### 원인
API 라우트가 에러 상황에서 JSON이 아닌 **HTML 에러 페이지**를 반환하고 있고, 클라이언트가 상태 코드 확인 없이 `res.json()`을 호출해서 파싱이 터진 것이다.

### 지시

**서버 측** — 분필을 소모하는 모든 API 라우트를 찾아 아래 형태로 통일한다.

```ts
// 잔액 부족
return NextResponse.json(
  { error: 'INSUFFICIENT_CHALK', required, balance },
  { status: 402 }
);

// 비로그인 / 토큰 만료
return NextResponse.json({ error: 'UNAUTHENTICATED' }, { status: 401 });
```

- 모든 라우트 핸들러를 `try/catch`로 감싸고, catch에서도 반드시 JSON을 반환한다. 예외가 밖으로 새어나가 Next.js 기본 HTML 에러 페이지가 뜨는 경로가 하나도 없어야 한다.
- **차감은 `runTransaction`으로 처리한다.** 현재 `getDoc` → 메모리 수정 → `updateDoc` 패턴(읽기-계산-쓰기)이면 전부 트랜잭션으로 교체할 것. 트랜잭션 안에서 잔액을 재확인하고, 부족하면 롤백한다.
- **이벤트 분필 우선 차감** 규칙을 유지하고, 만료된 이벤트 분필은 차감 대상에서 제외한다.
- 기능 실행이 실패하면 차감된 분필을 되돌린다(또는 성공 확정 후 차감).

**클라이언트 측** — 공용 fetch 래퍼를 만든다 (`src/lib/apiClient.ts`).

```ts
async function apiCall(url, options) {
  const res = await fetch(url, options);
  const contentType = res.headers.get('content-type') ?? '';

  if (!contentType.includes('application/json')) {
    throw new AppError('SERVER_ERROR', '일시적인 오류가 발생했어요. 잠시 후 다시 시도해주세요.');
  }
  const data = await res.json();
  if (!res.ok) throw new AppError(data.error, data);
  return data;
}
```

분필을 쓰는 모든 호출 지점을 이 래퍼로 교체한다.

**UI** — `INSUFFICIENT_CHALK` 수신 시 모달 표시.

```
🖍️ 분필이 부족해요

이 기능은 분필 2개가 필요한데, 현재 0개예요.

[ 분필 충전하기 ]   [ 닫기 ]
```

- 충전 버튼은 `/shop`으로 이동하되, **원래 하려던 작업을 쿼리스트링이나 세션스토리지에 담아** 충전 완료 후 돌아왔을 때 이어서 진행할 수 있게 한다.
- **사전 차단이 더 중요하다.** 기능 실행 버튼에 필요 분필 수를 미리 표시하고(`자리표 PDF 저장 🖍️1`), 잔액이 부족하면 버튼을 누르기 전에 이미 다른 상태로 보이게 한다. 실행 후 실패는 최악의 UX다.
- `isChalkEnabled()`가 false면 이 모든 흐름은 건너뛰고 그냥 실행된다.

---

## Task 6. Lemon Squeezy 결제 연동

### 설계 원칙

**결제 프로바이더를 추상화 레이어 뒤에 둔다.** 향후 원화 결제(포트원/토스페이먼츠) 또는 Stripe Managed Payments로 전환할 가능성이 있으므로, Lemon Squeezy 고유 코드가 앱 전체에 흩어지면 안 된다.

```
src/lib/payments/
  types.ts          — Product, CheckoutSession, PaymentEvent 등 공용 타입
  index.ts          — createCheckout(), 프로바이더 선택
  lemonsqueezy.ts   — LS 전용 구현
app/api/webhooks/lemonsqueezy/route.ts
```

### 상품 구성

| 종류 | LS 상품 타입 | 지급 |
|---|---|---|
| 분필 팩 (소모성) | One-time | 분필 N개 즉시 지급 |
| 구독 (플러스) | Subscription | 등급 승격 + 매월 분필 기본 지급 |

### 구현 요구사항

**1) 체크아웃 생성**

- 서버에서 LS Checkout API로 세션을 만들고 URL을 반환한다. (클라이언트에서 API 키를 절대 노출하지 않는다)
- `checkout_data.custom` 에 **Firebase UID**를 반드시 넣는다. 웹훅에서 사용자를 매칭할 유일한 수단이다.
- 이메일도 함께 전달해 결제 화면에서 자동 입력되게 한다.

**2) 웹훅 처리** (`/api/webhooks/lemonsqueezy`)

- **서명 검증 필수.** `X-Signature` 헤더를 signing secret으로 HMAC-SHA256 검증한다. 검증 전에는 어떤 처리도 하지 않는다. Next.js App Router에서 raw body를 얻으려면 `await req.text()`를 사용하고, 검증 후에 파싱한다.
- 처리할 이벤트: `order_created`, `subscription_created`, `subscription_updated`, `subscription_cancelled`, `subscription_expired`, `subscription_payment_success`
- **멱등성 보장.** LS는 웹훅을 재전송할 수 있다. `payments/{lemonSqueezyEventId}` 문서를 트랜잭션 안에서 생성하고, 이미 존재하면 즉시 종료한다. 중복 지급이 발생해선 안 된다.
- 분필 지급도 같은 트랜잭션 안에서 처리한다.
- 검증 실패는 401, 처리 실패는 500을 반환한다 (LS가 재시도하도록).

**3) 결제 기록**

```
payments/{eventId}
  uid, type: 'chalk_pack' | 'subscription'
  amount, currency, chalkGranted
  lemonSqueezyOrderId, lemonSqueezyCustomerId
  status, createdAt
```

**4) 환경변수** — `.env.example`에 추가하고 필요한 값을 정리해서 알려줄 것

```
LEMONSQUEEZY_API_KEY=
LEMONSQUEEZY_STORE_ID=
LEMONSQUEEZY_WEBHOOK_SECRET=
LEMONSQUEEZY_VARIANT_CHALK_STARTER=
LEMONSQUEEZY_VARIANT_CHALK_STANDARD=
LEMONSQUEEZY_VARIANT_SUBSCRIPTION_MONTHLY=
```

**5) `/shop` 페이지** — 현재 `alert("결제 기능 준비 중")` 만 있으므로 실제 구현으로 교체한다. 가격은 상수 파일로 분리해 나중에 쉽게 바꿀 수 있게 한다.

**6) 구독 관리** — 구독자가 결제 수단 변경·해지를 할 수 있도록 LS Customer Portal 링크를 마이페이지에 노출한다.

### 완료 후 별도 안내 요청

Lemon Squeezy 대시보드에서 사람이 직접 해야 하는 설정(상품/변형 생성, 웹훅 URL 등록, 테스트 모드 확인 절차)을 **체크리스트로 정리해서 출력**할 것.

---

## Task 7. 랜덤 뽑기 연출 강화 (별도 세션 권장)

### 목표
현재는 이름이 깜빡이다 멈추는 슬롯머신 방식이라 긴장감이 부족하다. 수업 중에 학생들이 화면을 보며 반응할 만한 연출이 필요하다.

### 판단 요청 사항

먼저 아래를 검토하고 **별도 앱 분리가 필요한지 판단해서 보고**하라. 결론부터 말하면 분리는 아마 불필요하다 — `next/dynamic`의 `ssr: false`로 물리 엔진을 별도 청크로 빼면 메인 번들에는 영향이 없다. 하지만 실제 번들 사이즈를 측정한 뒤 판단할 것.

### 구현할 연출 (우선순위 순)

**1) 사다리타기** — 가장 먼저 구현. 한국 교실에서 문화적으로 친숙하고, 캔버스만으로 가볍게 구현된다.
- 학생 이름을 위에 배치, 아래에 당첨/순서 배치
- 가로줄 랜덤 생성, 경로를 애니메이션으로 한 줄씩 따라 내려감
- 여러 명 동시 추적 시 색깔 구분

**2) 레이싱** — 이름표가 트랙을 달리고, 랜덤 가속/감속으로 순위가 뒤바뀐다. 결승선 근처에서 역전이 나오게 가중치를 조정하면 긴장감이 크다. 순위 전체를 뽑기 때문에 **발표 순서 정하기**로도 쓰인다.

**3) 플링코(볼 드롭)** — `matter.js`를 `next/dynamic`으로 지연 로딩. 이름표가 핀 사이를 튕기며 떨어져 아래 슬롯에 안착. 여러 개를 동시에 떨어뜨려 도착 순서로 순위를 매길 수 있다.

**4) 카드 뒤집기** — 뒷면 카드가 깔리고, 선생님이나 학생이 직접 카드를 클릭하면 뒤집히며 이름이 나온다. 학생 참여형이라 반응이 좋다.

### 공통 요구사항

- **결과는 애니메이션 전에 미리 결정한다.** 물리 시뮬레이션의 결과를 그대로 쓰면 재현이 안 되고 검증도 어렵다. 공정한 난수로 결과를 먼저 뽑고, 애니메이션은 그 결과로 향하도록 연출한다.
- 연출 방식은 설정에서 선택 가능하게 하고, 기존 슬롯머신도 "빠른 뽑기"로 남긴다.
- 전체화면 모드 필수 (교실 프로젝터/TV 출력용). 이름 글자 크기가 뒷자리에서도 보여야 한다.
- 효과음 on/off 토글. 기본은 on, 볼륨 조절 가능.
- 이미 뽑힌 학생 자동 제외 기능 유지, "전체 초기화" 버튼 제공.
- 애니메이션 스킵 버튼 (수업 시간이 촉박할 때).
- **비로그인에서도 사용 가능하게 유지한다.** 이게 신규 유입 훅이다.

---

## 완료 기준

각 태스크 종료 시 아래를 보고할 것.

- [ ] 수정한 파일 목록 (전체 경로)
- [ ] 원인 분석 요약 (버그 태스크의 경우)
- [ ] 실행한 테스트와 결과
- [ ] Firebase 콘솔에서 사람이 직접 해야 할 작업 (있다면)
- [ ] 새로 추가된 환경변수 (있다면)
- [ ] 기존 데이터 마이그레이션 필요 여부와 처리 방식
